@{
    ViewData["Title"] = "Cancel";
}

<div class="cancel-container">
    <h2>Payment Canceled</h2>
    <p>Your transaction was not completed. Please try again.</p>
    <a href="https://localhost:7289/" class="btn-retry">Try Again</a>
</div>

<style>
    /* Apply the color palette */
    body {
        font-family: 'Poppins', sans-serif;
        background-color: #F2EFE7; /* Lightest shade */
        color: #2973B2; /* Dark Blue */
        text-align: center;
        margin: 0;
        padding: 0;
    }

    .cancel-container {
        background-color: #9ACBD0; /* Light Blue */
        padding: 40px;
        border-radius: 12px;
        box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.1);
        display: inline-block;
        margin-top: 100px;
    }

    h2 {
        color: #2973B2; /* Dark Blue */
        font-size: 28px;
        font-weight: bold;
        margin-bottom: 10px;
    }

    p {
        font-size: 18px;
        color: #48A6A7; /* Medium Blue */
    }

    /* Button Styling */
    .btn-retry {
        display: inline-block;
        margin-top: 20px;
        background-color: #2973B2;
        color: white;
        padding: 12px 24px;
        border-radius: 8px;
        text-decoration: none;
        font-size: 16px;
        transition: 0.3s ease-in-out;
    }

        .btn-retry:hover {
            background-color: #48A6A7;
        }
</style>
